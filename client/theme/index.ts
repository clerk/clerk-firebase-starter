import { extendTheme } from "@chakra-ui/react";

export const defaultTheme = extendTheme({
  fontSizes: {
    lg: "18px",
  },
  colors: {
    text: {
      dark: "#111827",
    },
    brand: {
      blue: "#335bf1",
    },
  },
});
